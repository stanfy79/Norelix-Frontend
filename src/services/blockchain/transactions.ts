// src/services/blockchain/transactions.ts
export const sendTransaction = async (to: string, amount: string) => {
  // Send transaction
  return {};
};

export const waitForTransaction = async (txHash: string) => {
  // Wait for confirmation
  return {};
};