// src/pages/Failed.tsx
import React from 'react';

const Failed: React.FC = () => {
  return (
    <div>
      <h1>Payment Failed</h1>
      {/* Error message */}
    </div>
  );
};

export default Failed;